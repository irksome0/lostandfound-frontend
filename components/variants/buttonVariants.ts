
export const basicVariants = {
    initial: {
        borderColor:"var(--border-secondary)",
        background: "var(--button-base)",
        color: "var(--text-secondary)",
      },
      hover: {
        borderColor:"var(--border-tertiary)",
        background: "var(--button-secondary)",
        color: "var(--text-tertiary)",
      },
      tap: {
        borderColor:"var(--border-tertiary)",
        background: "var(--button-tertiary)",
        color: "var(--text-tertiary)",
      },
}

export const alternateButtonVariants = {
    initial: {
        borderColor:"var(--border-base)",
        background: "var(--button-alt-base)",
        color: "var(--text-primary)",
      },
      hover: {
        borderColor:"var(--border-base-secondary)",
        background: "var(--button-alt-secondary)",
        color: "var(--text-quaternary)",
      },
      tap: {
        borderColor:"var(--border-base-secondary)",
        background: "var(--button-alt-tertiary)",
        color: "var(--text-quaternary)",
      },
}